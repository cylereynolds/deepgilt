"""Deepgilt engine skeleton v0.

Loads the design data in ../data and provides the core systems from DESIGN.md:
stats (§2), combat (§9), items + geas-words (§8), and the bind/geas interpreter (§7).
Run the end-to-end selftest with:  python3 -m engine
"""
